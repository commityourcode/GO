package main

import "fmt"

// Prints out "Super Mario 3"
// adds blank lines to separate the package and import declarations.
// It also adds some spacing into the variable assignment and the function parameters.
func main() {
	game_version := 3
	fmt.Printf("Super Mario %s\n", game_version)
}
